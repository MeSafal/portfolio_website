name: SFTP_Deploy_Portfolio

on:
  push:
    branches:
      - main

jobs:
  sftp-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Sync files via SFTP
        uses: atmoz/sftp-deploy@v1
        with:
          server: etanepal.com
          username: cicdtest
          password: X9v!qT7r#Lp2Zf
          local-dir: ./
          remote-dir: /home/etanepal/test.etanepal.com/
